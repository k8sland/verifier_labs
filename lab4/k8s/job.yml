apiVersion: batch/v1
kind: Job
metadata:
  name: lab4
spec:
  template:
    spec:
      serviceAccount: k8sland
      containers:
        - name: lab
          image: k8sland/verifier:v1.0.0
          imagePullPolicy: IfNotPresent
          command: ["/bin/verifier", "--u=fernand", "--l=4", "--s"]
          volumeMounts:
            - mountPath: /assets
              name: assets
      restartPolicy: Never
      volumes:
        - name: assets
          secret:
            secretName: lab4
  backoffLimit: 4
